{% extends "schedule/index.njk" %}

{% block schedule %}
  {% for appointment in appointments %}
    <li class="provider">
      <div>
        <img src="/files/{{ appointment.provider.avatar }}" />
        <strong>{{ appointment.provider.name }} <br> {{ appointment.hour }}</strong>
      </div>
    </li>
  {% endfor %}
{% endblock %}
